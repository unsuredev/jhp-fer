import { Page } from "components/Page";
import { Agent } from "features/agent/agent";

export const AgentPage = () => {
    return (
        <Page title="Jaman HP Gas| Agent">
            <Agent />
        </Page>
    );
};
